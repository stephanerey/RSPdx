# Project Profile

> **PRD Policy:** **PROJECT (editable)** — Fill and update this file for the current project.

**Status:** Draft  
**Owner:** <your name>  
**Last updated:** YYYY-MM-DD

## Project identity
- Project name:
- Short description:
- Repository / workspace:
- Main stakeholders:

## Project family
- Type: `<web_app / backend_service / desktop_app / python_tool / cli / library / c_embedded / mixed>`
- Lifecycle: `<greenfield / refactor / maintenance / migration / research>`
- Criticality: `<low / medium / high / safety-sensitive>`
- Delivery mode: `<prototype / internal tool / production / long-term maintained>`

## Domain intensity
- none / low / medium / high
- if medium or high, fill `06_domain/` before asking Codex to implement domain-heavy features

## Selected packs
List the packs intentionally selected for import into the project PRD.
- Core only: `<yes/no>`
- `packs/web_app`: `<yes/no>`
- `packs/python_desktop`: `<yes/no>`
- `packs/c_embedded`: `<yes/no>`
- `packs/backend_service`: `<yes/no>`
- `packs/cli_library`: `<yes/no>`
- `packs/data_ml`: `<yes/no>`
- Other custom pack: `<name or n/a>`

## Target environment
- Platforms / OS:
- Runtime / language:
- Hardware context:
- Deployment model:
- Offline / online expectations:

## Key constraints
- Budget / team / maintenance constraints:
- Performance / timing constraints:
- Security / privacy constraints:
- Regulatory / safety constraints:

## Project-specific notes
- Why this pack selection is appropriate:
- What is intentionally out of scope for this project:
- Which documents should be treated as priority entry points:
