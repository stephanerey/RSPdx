# AGENTS.override.md

## Local subtree rules
Use this file only when a subdirectory genuinely needs different rules from the repository root.

- Local build/test command: `<fill>`
- Local safety constraints: `<fill>`
- Local architecture rules: `<fill>`
- Local docs that must be read first: `<fill>`

## Override intent
State clearly why this subtree needs an override and which root rules still apply.
