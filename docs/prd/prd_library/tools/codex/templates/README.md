# Codex Templates

Use these files as starting points inside a real project repository.

## Intended placement
- `AGENTS.md` — repository root
- `AGENTS.override.md` — optional nested directory override
- `PLANS.md` — repository root or `.agents/PLANS.md`, depending on team convention

## Important rule
These are starter templates. Keep them short, concrete, and project-specific once instantiated.
